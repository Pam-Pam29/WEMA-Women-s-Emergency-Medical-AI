# WEMA — Hugging Face Spaces inference dependencies
# Push this file as requirements.txt inside your HF Space repo

# API + UI
fastapi==0.111.0
uvicorn==0.30.0
gradio==4.36.0
pydantic==2.7.0

# RAG pipeline
langchain==0.2.0
langchain-community==0.2.0
langchain-text-splitters==0.2.0
chromadb==0.5.0
sentence-transformers==3.0.0

# LLM
groq==0.9.0
langchain-groq>=0.1.0

# PDF processing (only needed if re-running ingest on the Space)
pypdf==4.2.0

# Utils
python-dotenv==1.0.0
numpy==1.26.0
